{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for BloomCode main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs-commands",
    "terminal-commands",
    "core:event:default"
  ]
}
